spring.application.name=no-dues-backend
server.port=8080

# default profile for local only
spring.profiles.active=dev

# common JPA settings
spring.jpa.open-in-view=false

# optional, not needed for custom health API
# management.endpoints.web.exposure.include=health