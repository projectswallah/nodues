spring.application.name=no-dues-backend
server.port=8080

# default profile for local only
spring.profiles.active=dev

# common JPA settings
spring.jpa.open-in-view=false

# for health check while deploying
management.endpoints.web.exposure.include=health